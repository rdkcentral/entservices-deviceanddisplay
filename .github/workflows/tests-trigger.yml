name: main-workflow

on:
  push:
    branches: [ main, develop, 'sprint/**', 'release/**' ]
  pull_request:
    branches: [ main, develop, 'sprint/**', 'release/**' ]

jobs:
  say-hello:
    runs-on: ubuntu-22.04
    uses: rdkcentral/entservices-deviceanddisplay/.github/workflows/L1-tests.yml@feature/RDKEMW-3513-yml
