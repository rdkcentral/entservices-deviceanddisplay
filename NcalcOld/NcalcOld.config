set (autostart ${PLUGIN_NCALCOLD_AUTOSTART})
set (preconditions Platform)
set (callsign "org.rdk.NcalcOld")

if(PLUGIN_NCALCOLD_STARTUPORDER)
set (startuporder ${PLUGIN_NCALCOLD_STARTUPORDER})
endif()

